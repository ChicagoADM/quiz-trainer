version: '3.8'

services:
  quiz:
    build: .
    container_name: quiz_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - quiz_data:/app
    restart: unless-stopped

volumes:
  quiz_data: